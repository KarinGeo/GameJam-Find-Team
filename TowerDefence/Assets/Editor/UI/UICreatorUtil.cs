using System.Collections;
using System.Collections.Generic;
using System.IO;
using UnityEngine;

public class UICreatorUtil
{
    public static void GenerateUIControllerFile(string filePath, string className)
    {
        StreamWriter streamWriter = null;
        if (File.Exists(Application.dataPath + "/Scripts/UI"))
        { 
            
        }
    }
}
